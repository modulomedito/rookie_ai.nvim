# rookie_ai.nvim

## Installation

Use lazy.nvim to install this plugin.

```lua
require("lazy").setup({
    {
        "modulomedito/rookie_ai.nvim",
        config = function()
            require("rookie_ai").setup()
        end,
    },
})
```
